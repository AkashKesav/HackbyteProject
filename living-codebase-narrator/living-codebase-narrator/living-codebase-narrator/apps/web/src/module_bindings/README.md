# Module bindings placeholder

This directory is the target for generated SpacetimeDB TypeScript bindings.

If you haven’t generated bindings yet, it stays as a placeholder so the web app still typechecks.

